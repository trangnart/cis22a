#include <iostream>
#include <vector>
using namespace std;

int main() {

   vector<int> binaryNum(8);
   int digitWeight;
   int decimalSum;
   int i;

   // Get user's binary number. Store leftmost bit in element 7, rightmost in 0.
   // Hint: for (...) {
      cin >> binaryNum.at(i);
   }

   /* Type your code here. */

   return 0;
}